# =============================================================================
# GraalVM Native Image auto-discovery configuration for Stirling-PDF
# =============================================================================
# GraalVM automatically discovers this file and all sibling JSON configs
# (reflect-config.json, jni-config.json, resource-config.json, etc.)
# from META-INF/native-image/ on the classpath.
#
# This eliminates the need for experimental -H:ReflectionConfigurationFiles=...
# flags in the build configuration.
# =============================================================================
# No additional Args needed — the JSON files in this directory are
# auto-discovered by the native-image builder.
